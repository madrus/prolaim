(function () {
    'use strict';

    angular.module('prolaim.config', []);

    angular.module('prolaim.config')
        .constant('defaultSettings', {
            language: 'ru'
        }
    );

})();
